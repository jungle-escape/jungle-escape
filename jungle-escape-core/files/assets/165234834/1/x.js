// var X = pc.createScript('x');

// // initialize code called once per entity
// X.prototype.initialize = function() {
//     // 이 부분에서 oncollisionstart 이벤트에 대한 리스너를 추가합니다.
//     this.entity.collision.on('collisionstart', this.onCollisionStart, this);
//     // this.entity.sound.addSlot('xsound');
// };

// // 충돌이 시작되었을 때 호출되는 함수
// X.prototype.onCollisionStart = function(event) {
//     this.entity.sound.play('xsound');
//     this.entity.enabled = false;
// };

// // update code called every frame
// X.prototype.update = function(dt) {
//     // 여기에 매 프레임마다 실행될 코드를 작성합니다.
// };

