var GlobalSound = pc.createScript('globalSound');

// initialize code called once per entity
// FloorAttack.prototype.initialize = function () {
//     pn.on(`${this.entity.name}`, () => {
//         if (!this.entity.sound.slot('sound').isPlaying) {
//             this.entity.sound.play("sound");
//         };
//     });
// }