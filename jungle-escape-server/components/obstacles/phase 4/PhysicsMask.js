var PhysicsMask = pc.createScript('physicsMask');

// PhysicsMask.attributes.add('mask', {type: 'number', default: 1});

// PhysicsMask.prototype.initialize = function() {
//     // mask 0b0001 && group 0b0000 : can collide
//     // mask 0b0001 && group 0b0001 : can't collide
//     this.entity.rigidbody.mask = this.mask;
// };
